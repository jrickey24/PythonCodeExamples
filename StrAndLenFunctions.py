# len() gets the length of the string. str() allows the integer value
# returned by the len() function to be treated as a string value
# in order to be concatenated to the  existing string value

print('Hello has ' + str(len('Hello')) + ' letters in it.')
